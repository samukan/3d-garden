{
  "name": "skill-garden",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "playwright:install": "playwright install chromium",
    "debug:browser": "playwright test -c playwright.debug.config.ts",
    "debug:browser:headed": "playwright test -c playwright.debug.config.ts --headed"
  },
  "dependencies": {
    "@babylonjs/core": "^8.12.1",
    "@babylonjs/loaders": "^8.54.3",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.5"
  }
}
